# Лабораторная работа 3: Основы работы с протоколом HTTP
##### Выполнил: ***Дьяконов Олег***
---

### Практические задания

- Установить и запустить `Postman`.
- Создать новую коллекцию с названием `HTTP_Lab`.
- Создать `GET-запрос` к публичному `API https://catfact.ninja/fact`.
- Выполнить `GET-запрос к API` стран и сравнить ответы сервера при наличии и отсутствии `query-параметра`.
- Создать `POST-запрос` и отправить данные в формате `JSON`.
- Выполнить `PUT-запрос` для обновления ресурса.
- Выполнить `DELETE-запрос` и повторить `GET-запрос` к этому ресурсу.
- Выполнить дополнительные запросы для проверки реакции сервера на нестандартные случаи.

---

### Ход выполнения

Работа выполнялась в **postman**

---

## 1. GET-запрос к API

- *В `postman` была создана новая коллекция **HTTP_Lab** и был сделан `GET-запрос к публичному API`:*
```
GET https://catfact.ninja/fact
```

- *Скриншот:*
<img width="1133" height="1280" alt="изображение" src="https://github.com/user-attachments/assets/e80bc09f-636e-4498-93ae-331ba500d25a" />

>>> *Был выполнен `GET-запрос к API https://catfact.ninja/fact` с использованием `Postman`. Сервер вернул статус-код **200 OK**, что означает успешное выполнение запроса. Ответ представлен в формате `JSON` и содержит случайный факт о кошках и его длину.*

---

## 2. Работа с query-параметрами

- *Выполняем `GET-запрос к API` стран `без query-параметров` и затем с параметром `fullText=true` для сравнения ответа сервера:*
```
GET https://restcountries.com/v3.1/name/Russia
```

- *Скриншот:*
<img width="1133" height="1280" alt="изображение" src="https://github.com/user-attachments/assets/03ec3391-5804-4d54-a3b6-a6e9fab6b00e" />

>>> *Выполнен `GET-запрос без query-параметров`. Сервер вернул данные о стране в формате `JSON`.*

- *Скрипт второго запроса:*
```
GET https://restcountries.com/v3.1/name/Russia?fullText=true
```

- *Скриншот:*
<img width="1180" height="1280" alt="изображение" src="https://github.com/user-attachments/assets/27672134-2251-4acd-bc3a-6c22345d4bed" />

>>> Добавлен query-параметр `fullText=true`. Параметр используется для точного поиска по полному совпадению названия страны.

---

## 3. Отправка данных с использованием POST

- *Выполняем `POST-запрос` для создания нового ресурса и передаем данные в теле в формате `JSON`:*
```
POST https://jsonplaceholder.typicode.com/posts
```

- *Тело запроса:*
```
{
  "title": "Test post",
  "body": "This is a test",
  "userId": 1
}
```

- *Скриншот:*
<img width="1140" height="1280" alt="изображение" src="https://github.com/user-attachments/assets/598cd804-4187-48af-a29a-06cdba0c636a" />

>>>*Выполнен `POST-запрос с JSON-телом`. Сервер вернул `статус-код 201 Created`, что означает успешное создание ресурса. В ответе был возвращён объект с добавленным идентификатором `id`.*

---

## 4. Модификация данных с использованием PUT

- *Выполняем `PUT-запрос` для обновления ресурса с идентификатором `1` и передаем новые данные в теле запроса:`
```
PUT https://jsonplaceholder.typicode.com/posts/1
```

- *Тело запроса:*
```
{
  "title": "Updated post",
  "body": "This is updated content",
  "userId": 1
}
```

- *Скриншот:*
<img width="1167" height="1280" alt="изображение" src="https://github.com/user-attachments/assets/9ddd74ab-d4ad-423f-84f5-72dc542cbfe6" />

>>> *Выполнен `PUT-запрос` к ресурсу `/posts/1`. Сервер вернул `статус-код 200 OK` и обновлённый `JSON-объект`, что означает успешное обновление данных.*

---

## 5. Удаление данных с использованием DELETE

- *Выполняем `DELETE-запрос` для удаления ресурса с идентификатором `1`, а затем повторяем `GET-запрос` к этому ресурсу:*
```
DELETE https://jsonplaceholder.typicode.com/posts/1
```

- *Скриншот:*
<img width="1176" height="1280" alt="изображение" src="https://github.com/user-attachments/assets/25ebb40b-3e62-42b6-8d3c-ab1a11e21d44" />

>>> *Выполнен `DELETE-запрос` к ресурсу `/posts/1`. Сервер вернул успешный `статус-код`. В случае использования тестового `API jsonplaceholder.typicode.com` удаление является имитацией.*

- *Скрипт:*
```
GET https://jsonplaceholder.typicode.com/posts/1
```

- *Скриншот:*
<img width="1176" height="1280" alt="изображение" src="https://github.com/user-attachments/assets/3bdc091a-e510-499b-9d07-5172bf2cf1c0" />

>>> *Повторно выполнен `GET-запрос` к тому же ресурсу. Так как используется `тестовый API`, данные могут возвращаться в исходном виде, поскольку изменения на сервере не сохраняются реально.

---

## 6. Проверка обработки нестандартных запросов

### 6.1 POST без тела запроса

- *Выполняем `POST-запрос` без тела, чтобы проверить реакцию сервера на пустой запрос:*
```
POST https://jsonplaceholder.typicode.com/posts
```

- *Скриншот:*
<img width="1245" height="1361" alt="изображение" src="https://github.com/user-attachments/assets/45064073-c86f-4b7b-9924-a4541eddf6f6" />


>>> *Выполнен `POST-запрос без тела`. Сервер обработал запрос и вернул ответ. Это показывает, как `API` реагирует на отсутствие данных `в body`.

### 6.2 POST с пустым JSON {}

- *Выполняем `POST-запрос` с пустым `JSON-объектом`:*
```
POST https://jsonplaceholder.typicode.com/posts
```

- *Тело запроса:*
```
{}
```

- *Скриншот:*
<img width="1167" height="1280" alt="изображение" src="https://github.com/user-attachments/assets/16fa2a9a-decb-4b97-9500-9a728e7d9222" />



>>> *Выполнен запрос с пустым `JSON{}`. Сервер принял запрос и вернул ответ, что позволяет проверить обработку пустого тела запроса

### 6.3 PUT к несуществующему ресурсу

- *Выполняем `PUT-запрос` к ресурсу, которого нет на сервере:
```
PUT https://jsonplaceholder.typicode.com/posts/999999
```

- *Тело запроса:*
```
{
  "title": "New title",
  "body": "New body",
  "userId": 1
}
```

- *Скриншот:*
<img width="1246" height="1369" alt="изображение" src="https://github.com/user-attachments/assets/65a7e7e9-34e0-4227-8e4b-ce4003648ac5" />

>>> Выполнен PUT-запрос к несуществующему ресурсу. Сервер обработал запрос и вернул ответ, демонстрирующий реакцию API на отстутствие объекта с указанным идентификатором.

---
# Вывод

В ходе лабораторной работы были изучены основы работы с **протоколом HTTP**, основные `методы REST API` и принципы использования `Postman`. Были получены практические навыки отправки **GET, POST, PUT и DELETE-запросов**, анализа ответов сервера, работы `JSON с query-параметрами`.
